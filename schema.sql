CREATE TABLE indeed_jobs(
	position VARCHAR,
	company VARCHAR,
	description VARCHAR,
	city VARCHAR,
	state VARCHAR
);
select * from indeed_jobs

CREATE TABLE glassdoor_jobs(
	position VARCHAR,
	company VARCHAR,
	description VARCHAR,
	city VARCHAR,
	state VARCHAR
);
